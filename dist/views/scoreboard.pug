extends layout

block content
	h1=title

	ul
		each score in score_list
			li
				p #{score.value}: #{(score.hasOwnProperty("user") ?  score.user.name : "Unknown")}, #{score.date_formatted}
		else
			li There are no scores